#include "main.h"

/**
 * _isalpha - returns the alphabet
 * @c: input
 * Description: The description
 * Return: 0
*/
int _isalpha(int c)
{
	if ((c >= 97 && c <= 122) || (c >= 97 && c <= 122))
		return (1);
	return (0);
}
