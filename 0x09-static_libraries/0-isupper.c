#include "main.h"
#include "string.h"
/**
 * _isupper - starting point
 * @c: the character
 * Description: The description
 * Return: 0
*/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	return (0);
}
